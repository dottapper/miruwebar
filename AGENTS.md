# AGENTS.md — miru-WebAR 向け AI オーケストレーション

## 目的

このリポジトリは **指揮官（オーケストレーター）** と **作業者（ワーカー）** の役割分担で進めます。

| 役割 | 責務 |
|------|------|
| **指揮官** | 要件整理、アーキテクチャ判断の維持、タスク分割、ワーカーへの委任、結果レビュー、`PROJECT_STATE.md` 更新判断 |
| **作業者** | 1 タスクずつ完遂、変更前の状態確認、必要ファイルのみ修正、検証実行、結果報告 |

## 必読ドキュメント（優先順）

作業開始前に、次をこの順で読むこと。チャット履歴は正としない。**リポジトリ内ドキュメントが正**。

1. `AGENTS.md`（本ファイル）
2. `PROJECT_STATE.md`（プロジェクトの永続メモリ）
3. `.cursor/rules/webar.mdc`（コーディング・UI・AR 固有ルール）
4. タスクに直接関係するソースのみ

人間向けの補助資料:

- `README.md` — セットアップ・機能概要
- `CONTRIBUTING.md` — PR・デザイン影響の運用
- `docs/routes.md` — ハッシュルート一覧

## 作業ルール

1. `PROJECT_STATE.md` に記載された決定事項を、明示的な報告なしに覆さない。
2. タスクに不要なファイルは変更しない。
3. 次は **指揮官へのエスカレーション必須**:
   - 新規 npm 依存の追加
   - ハッシュルーティング（`#/…`）の設計変更
   - IndexedDB スキーマ / マイグレーション方針の変更
   - 認証（`AUTH_ENABLED`）の挙動変更
   - Firebase / Vercel API 契約の変更
   - AR エンジン切替（AR.js / WebXR）の方針変更
4. 受け入れ基準を満たす **最小の安全な変更** を優先する。
5. 完了宣言前に、本リポジトリに存在する検証コマンドを実行する。
6. 1 タスク = 1 関心事。複数機能を混在させない。
7. `PROJECT_STATE.md` は、決定・現状・リスク・次タスクが **実質的に変わったときだけ** 更新する。
8. API キー・パスワード・顧客データをドキュメントやコミットに含めない。

## 指揮官プロトコル

1. `AGENTS.md` と `PROJECT_STATE.md` を読む。
2. 要求を分類する:
   - 小さな局所修正
   - 複数ファイルにまたがる機能
   - アーキテクチャ判断
   - 調査 / デバッグ
3. 複数ステップの作業は、実装前に短いタスク計画を立てる。
4. サブエージェントが使える場合、実装・テスト・調査はワーカーに委任する。
5. ワーカーには下記 **Worker Brief** 形式で 1 タスクずつ渡す。
6. 依存タスクは順次実行。同一ファイルを触るタスクは並列にしない。
7. ワーカー結果を受け入れ基準と `PROJECT_STATE.md` の決定事項でレビューする。
8. 不確実な設計判断は、黙って広い変更をせずエスカレーションする。

## Worker Brief 形式

ワーカーへの指示は必ず次の形式を含める。

### Goal
達成すべき具体的な 1 成果。

### Context
なぜこのタスクが必要か（最小限の背景）。

### Relevant files
読む / 変更してよいファイル・ディレクトリ。

### Do not modify
スコープ外のファイル・システム・境界。

### Acceptance criteria
客観的な完了条件。

### Validation
実行するコマンド（**存在するもののみ**）:

```bash
npm run test:run      # ユニットテスト
npm run build:verify  # クリーンビルド + 検証
npm run check:start   # スタート画面スモーク
npm run dev           # 手動確認が必要な場合
```

※ 本リポジトリに `lint` / `typecheck` スクリプトはない。

### Required final report
- 変更ファイル一覧
- 変更内容の要約
- 実行した Validation と結果
- 残リスク / ブロッカー
- `PROJECT_STATE.md` 更新が必要か（Yes/No と理由）

## エスカレーション条件（作業者 → 指揮官）

- `PROJECT_STATE.md` の決定と矛盾する
- アーキテクチャ判断が必要
- 新規依存が必要
- ルーティング・ストレージ・認証・外部 API の契約変更の可能性
- テスト結果が設計問題を示唆
- スコープ内で完遂できない

## Definition of Done

- 受け入れ基準を満たしている
- Validation を実行済み、または実行不能の理由を報告済み
- 無関係ファイルを変更していない
- 最終レポートを返している

## UI / デザイン変更時の追加ルール

`CONTRIBUTING.md` に従う:

- 共通レイアウト・デザイントークンを継承する
- 新規ページには戻るボタンと現在地表示を付ける
- 他ページへ波及する CSS 変更は `design-impact` 相当の確認を前提とする
- ルート追加・変更時は `docs/routes.md` を更新する

## ドキュメントの役割分担（重複を避ける）

| ファイル | 役割 |
|----------|------|
| `AGENTS.md` | AI の役割分担・作業手順 |
| `PROJECT_STATE.md` | 現状・決定事項・作業キュー・リスク |
| `.cursor/rules/webar.mdc` | コーディング規約・Three.js/AR 実装ルール |
| `README.md` | 人間向けセットアップ・機能説明 |
| `CONTRIBUTING.md` | 人間向け PR / デザイン運用 |
| `docs/*.md` | 個別トピックの詳細（Firebase, Vercel, 監査など） |
