# WebAR プロジェクト改善プラン

**最終更新**: 2026-02-01
**ステータス**: メンテナンスフェーズ

---

## 📊 現状サマリー

### ✅ 完了済み項目

#### インフラ・基盤
- ✅ 実装分断の解消（擬似検出フロー停止、実エンジン経路への一本化）
- ✅ ステート遷移修正（`launch_requested→permission_prompt→camera_starting`）
- ✅ マーカー(.patt/画像)優先ローディング
- ✅ GLTFLoader レース解消（再初期化ロジック追加）
- ✅ `cleanup()`メソッド実装
- ✅ AR状態機械（`ar-state-machine.js`）の実装・インポート完了
- ✅ デバッグコード除去（XXXXXログ等）

#### 認証・セキュリティ
- ✅ Firebase/Google認証を削除、Middleware認証に統一
- ✅ シンプルなパスワード認証の実装
- ✅ Vercel用認証API追加（api/auth/login, check, logout）
- ✅ ログイン画面の改善（認証設定の自動リダイレクト、パスワード表示切替）

#### UI/UX
- ✅ カスタムガイド優先表示（`guideScreen.markerGuide.*`の絶対URL化）
- ✅ QRスキャン後のUI設定優先順位修正
- ✅ QRスキャン後のマーカーARビューでUI設定を正しく適用
- ✅ 診断ページとスモークテストページの実装
- ✅ Feature flags による UI 制御
- ✅ 削除確認ダイアログのUI改善
- ✅ アニメーション再生時のモデル未選択通知

#### コード品質
- ✅ プロジェクト整理・不要ファイル削除
- ✅ 単一フェッチ・重複検知の仕組み導入
- ✅ `logger.js`（統一ロガー）実装済み
- ✅ ログ出力の統一（直接console.logを削除）
- ✅ `ar-viewer.js`を3675行→3150行に削減

#### デプロイ
- ✅ Firebase Storage CORS設定・書き込み権限設定
- ✅ Firebase公開時のproject.jsonスキーマ修正
- ✅ Vercelデプロイ設定

---

## 🟢 P1: 改善タスク（中期対応）

### P1-1: ar-viewer.js のさらなる責任分離

**現状**: `ar-viewer.js`が3,150行（以前は3,675行）

**対応**:
新規ファイル作成を検討:
- `src/services/project-loader.js` - プロジェクト読み込み・正規化
- `src/services/marker-pipeline.js` - マーカー処理パイプライン
- `src/services/ar-session-manager.js` - ARセッション管理

**目標**: 3,150行 → 約1,000行に削減

---

### P1-2: ストレージ API の統一

**現状**:
- `src/api/projects.js` と `src/api/projects-new.js` が併存

**対応**:
- 2つのファイルを統合
- IndexedDB ＋軽量 localStorage の一貫 API を提供

---

### P1-3: 開発用コードの整理

**現状**:
- `src/dev/takeover-viewer-standalone.js` がまだ存在

**対応**:
- 開発用コードを`dev/`フォルダに集約、または不要なら削除
- 本番ビルドから除外されていることを確認

---

## 🔵 P2: 長期タスク（保守性/品質向上）

### P2-1: 大規模ファイルの分割

- `src/views/editor.js`を責務単位に分割
- 単体テスト可能な粒度へ整理

### P2-2: パフォーマンス/メモリ

- Three.js リソース解放の徹底（dispose/テクスチャ/ジオメトリ）
- バンドルサイズ監視とチャンク設計の見直し

### P2-3: Fetch 最適化

- HEAD リクエストを廃止
- GET リクエストで存在確認と取得を同時実行
- キャッシュ活用

---

## 📋 運用・保守タスク

### リポジトリ衛生
- `public/projects/**`（生成物）は`.gitignore`対象を検討
- 大容量バイナリ（GLB/PSD）は LFS または外部配信を検討
- テスト HTML は`examples/`等に集約

### ビルド設定の安定化
- `vite.config.js`の`Date.now()`付与でビルドが非決定になる点を解消
- `optimizeDeps.force: true`を必要時に限定

---

## ✅ 検証基準

### テスト環境
- iPhone Safari（最新iOS）
- Android Chrome（最新）
- PC Chrome（開発用）

### 成功基準
- [ ] Network: project.json 1回のみ
- [ ] Console: エラー0件、`[FETCH] DUPLICATE` 0件
- [ ] Flow: スタート → ローディング → ガイド → AR
- [ ] destroy() 呼び出しでエラーなし
- [ ] マーカー検出が正常に動作
- [ ] カスタムガイドが表示される
- [ ] 認証フローが正常に動作

### テストURL
```
https://localhost:3000/?src=/projects/sample-keep-me/project.json#/viewer
```

---

## 🚨 緊急時対応

### ロールバック手順
```bash
git revert HEAD
npm run build && npm run preview
```

### Feature Flag による無効化
```javascript
// src/config/feature-flags.js
export const DEV_FORCE_SCREENS = false;
export const DEV_VERBOSE_LOGS = false;
```

---

## 📚 参考ドキュメント

- [routes.md](docs/routes.md) - ルート定義
- [vercel-deployment.md](docs/vercel-deployment.md) - Vercelデプロイ手順
- [firebase-storage-rules.md](docs/firebase-storage-rules.md) - Firebase Storage設定
- [firebase-cors-setup.md](docs/firebase-cors-setup.md) - CORS設定

---

**最終更新**: 2026-02-01
